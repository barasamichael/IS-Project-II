# Backend Overview

SettleBot backend is responsible for:

- Exposing API endpoints (`api/`)
- Core services for document processing, embeddings, evaluation, and chat (`services/`)
- Configuration management (`config/`)
- Shared utilities (`utilities/`)

This section documents the high-level design and links into the API reference generated from the code.
